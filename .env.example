# Optional for publishing signed GitHub releases.
GH_TOKEN=

# Code-signing credentials are supplied only in CI.
CSC_LINK=
CSC_KEY_PASSWORD=

# Google Cloud Desktop OAuth client ID. When set at build time, Gmail becomes a one-click connection for users.
MAIN_VITE_GOOGLE_CLIENT_ID=
